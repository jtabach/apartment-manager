'use strict';

var express = require('express');
var router = express.Router();

var Apartment = require('../models/apartment');

router.get('/', function(req, res) {

}) // apartments page

router.get('/:appId') // single apartment info





module.exports = router;